<view class="nav-bar">
    <view class="on-left iconfont {{latest ? 'disable' : ''}}" bindtap="onLeft"></view>
    <text>{{title}}</text>
    <view class="on-right iconfont {{first ? 'disable' : ''}}" bindtap="onRight"></view>
</view>
