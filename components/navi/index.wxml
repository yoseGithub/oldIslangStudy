<view class="nav-bar">
    <view class="on-previous iconfont {{first ? 'disable' : ''}}" bindtap="onPrevious"></view>
    <text>{{title}}</text>
    <view class="on-next iconfont {{lastest ? 'disable' : ''}}" bindtap="onNext"></view>
</view>
